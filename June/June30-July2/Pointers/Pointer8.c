#include <stdio.h>

int main(int argc, char *argv[])
{
	char str[] = "abcdabcd";
	int i, j, k, l;
	char *w, *x, *y, *z;

	for(i=0;i<4;i++){
		w = &str[i];
		*w = str[i]
		for(j=0;j<4;j++){
			x = &str[j+1];
			*x = str[j+1];
			for(k=0;k<4;k++){
				y = &str[k+2];
				*y = str[k+2];
				for(l=0;l<4;k++){
					z = &str[l+3];
					*z = str[l+3];


	printf("%c", *p);

	return 0;
}
				
